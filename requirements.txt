# Multimodal Video Search — full deployment deps
# (this root requirements.txt is what Hugging Face Spaces installs)

# Core ML
torch>=2.1.0
open-clip-torch>=2.24.0
numpy>=1.24.0
Pillow>=10.0.0

# Video / vision
opencv-python-headless>=4.8.0
scenedetect[opencv]>=0.6.3

# Audio (graceful-degrade if missing)
faster-whisper>=1.0.0

# OCR (graceful-degrade if missing)
rapidocr-onnxruntime>=1.3.0

# Web UI
gradio>=6.0.0

# Infra
psutil>=5.9.0
tqdm>=4.66.0
